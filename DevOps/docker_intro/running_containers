# list running containers
docker ps

# list all containers that have been run before
docker ps -a

# list all downloaded images
docker images

# run bash interactively in centOS container 
docker run -it centos:latest /bin/bash

# run nginx in detached mode
docker run -d nginx:latest

# run elinks to view index page in nginx container
elinks http://172.17.0.2:80